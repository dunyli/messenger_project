/*
 * client_config.h — конфигурация C-клиента мессенджера
 */

#ifndef CLIENT_CONFIG_H
#define CLIENT_CONFIG_H

#define SERVER_IP       "127.0.0.1"     // IP-адрес сервера (по умолчанию localhost)
#define SERVER_PORT     8080            // порт сервера
#define BUFFER_SIZE     4096            // размер буфера для сообщений

#endif // CLIENT_CONFIG_H